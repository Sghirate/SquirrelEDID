<UserControl x:Class="SquirrelEDID.View.PromptTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Border HorizontalAlignment="Center" VerticalAlignment="Center" CornerRadius="5" Background="White" Padding="10" BorderBrush="Black" BorderThickness="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="BLABLALBALBALBALBLALBA"/>
                <Button Grid.Row="1" HorizontalAlignment="Right" Content="OK" Click="Button_Click" Padding="5,2,5,2"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
