<UserControl x:Class="SquirrelEDID.View.WelcomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:utils="clr-namespace:SquirrelEDID.Utilities"
             DataContext="{Binding Source={StaticResource ioc}, Path=Repository[WelcomeViewModel]}"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{utils:I18N Welcome_Part1}" FontSize="24" Margin="10" TextAlignment="Center"/>
        <TextBlock Grid.Row="1" Text="{utils:I18N Welcome_Part2}" TextWrapping="Wrap" Margin="10"/>
        <Button Grid.Row="2" HorizontalAlignment="Right" Background="Transparent" Command="{Binding StartCommand}">
            <TextBlock Text="{utils:I18N Welcome_Continue}"/>
        </Button>
        <Button Grid.Row="2" HorizontalAlignment="Left" Background="Transparent" Command="{Binding AboutCommand}">
            <TextBlock Text="{utils:I18N Welcome_About}"/>
        </Button>
    </Grid>
</UserControl>
