<Window x:Class="SquirrelEDID.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:c="clr-namespace:SquirrelEDID.View.Controls"
        xmlns:utils="clr-namespace:SquirrelEDID.Utilities"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Margin" Value="2"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </Window.Resources>
    <Grid Background="Azure">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <c:SlideControl Grid.Row="0" View="{Binding Source={StaticResource ioc}, Path=Repository[WelcomeView]}"/>
        <Grid Grid.Row="1" Background="AliceBlue">
            <TextBlock Margin="5" Text="{Binding tooltip}"/>
        </Grid>
    </Grid>
</Window>
